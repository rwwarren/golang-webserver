this is the readme

tarball command reminder
tar czf timeserver.tar.gz ./css490


For assignment 5:

compile and run "timeserver" and "authserver"
Then compile and run "loadgen"
Each component has a readme and a makefile
